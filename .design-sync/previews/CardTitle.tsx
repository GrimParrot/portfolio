import { Card, CardHeader, CardTitle } from 'portfolio'

export function Default() {
  return (
    <Card style={{ maxWidth: 360 }}>
      <CardHeader>
        <CardTitle>Onboarding redesign</CardTitle>
      </CardHeader>
    </Card>
  )
}
